import { OnlyNumberRateDirective } from './only-number-rate.directive';

describe('OnlyNumberRateDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberRateDirective();
    expect(directive).toBeTruthy();
  });
});
